# Optional. Set ONE provider key to run the copilot against a real model.
# With none set the app runs in demo mode (scripted agent, no network calls).
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
# Override the model string (see CopilotKit BuiltInAgent model list), e.g. openai/gpt-4.1-mini
# MOODBOARD_MODEL=
