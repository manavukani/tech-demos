# Optional. Set ONE of these to run against a real model.
# With neither set the app runs in mock mode (scripted generator, no network calls).
OPENAI_API_KEY=
# AI_GATEWAY_API_KEY=
# Override the model. OpenAI: a model id (default gpt-4o-mini). Gateway: provider/model (default anthropic/claude-haiku-4.5).
# JSON_RENDER_MODEL=
